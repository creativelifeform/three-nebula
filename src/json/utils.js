export const isTypeZone = ({ type }) => type === 'zone';
export const isTypeBody = ({ type }) => type === 'body';
export const isTypeLife = ({ type }) => type === 'life';
export const isTypeRadius = ({ type }) => type === 'radius';
export const isTypeMass = ({ type }) => type === 'mass';
